/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "links.papareact.com",
      "image.tmdb.org",
      "images.squarespace-cdn.com",
      "upload.wikimedia.org"
    ],
  }
}

module.exports = nextConfig
